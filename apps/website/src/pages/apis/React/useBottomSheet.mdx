import { CodePlayground } from "../../../components/CodePlayground/CodePlayground.tsx";

## `useBottomSheet`

`BottomSheet` component is controlled by `UseBottomSheetReturn`(the return type of `useBottomSheet`), so they go hand-in-hand.
Check out the [UseBottomSheetReturn](/apis/React/UseBottomSheetReturn/Methods) for use cases and examples.

### Interface

```ts
useBottomSheet: (props: UseBottomSheetProps) => UseBottomSheetReturn;
```

### Example

<CodePlayground
  code={`import { BottomSheet, useBottomSheet } from "@plainsheet/react";
    
export default function App() {
  const bottomSheet = useBottomSheet({
    // props to control your Bottom Sheet.
  });
  return (
    <div style={{ textAlign: "center", marginTop: "2rem" }}>
      <BottomSheet {...bottomSheet.props} >
        &nbsp; Your content goes here 🦭
      </BottomSheet>
      <button onClick={bottomSheet.open}>
        Open
      </button>
    </div>
  );
}
`}
/>

```jsx /useBottomSheet/ copy
import { useState } from "react";
import { BottomSheet } from "@plainsheet/react";

export default function Home() {
  const bottomSheet = useBottomSheet({
    // props to control your Bottom Sheet.
  });

  return (
    <section>
      <BottomSheet {...bottomSheet.props}>Your content goes here</BottomSheet>

      <button onClick={bottomSheet.open}>Open</button>
    </section>
  );
}
```
