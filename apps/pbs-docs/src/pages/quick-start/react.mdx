# Quick Start Guide

## Installation

### Using NPM

```shell copy
npm install @plainsheet/react
```

## Getting Started

### Using NPM

```ts copy
import { useState } from "react";
import { ReactPlainBottomSheet } from "@plainsheet/react";

export default function Home() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <section>
      <button
        onClick={() => {
          setIsOpen(true);
        }}
      >
        Open
      </button>
      <ReactPlainBottomSheet isOpen={isOpen} setIsOpen={setIsOpen}>
        Your content goes here
      </ReactPlainBottomSheet>
    </section>
  );
}
```
